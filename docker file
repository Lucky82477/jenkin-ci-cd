# Use Tomcat 10 with JDK 21 on Alpine Linux (lightweight)
FROM tomcat:10.1-jdk21-alpine

# Optional: Maintainer info
LABEL maintainer="admin@example.com"

# Remove the default webapps to start clean
RUN rm -rf /usr/local/tomcat/webapps/*

# Copy your .war file from your local machine/Jenkins workspace into the container
# Replace 'your-app.war' with your actual file name
COPY target/your-app.war /usr/local/tomcat/webapps/ROOT.war

# Expose port 8080
EXPOSE 8080

# Start Tomcat
CMD ["catalina.sh", "run"]
